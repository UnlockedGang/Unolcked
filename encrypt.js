const url = 'https://www.google.com';  // Reemplaza con tu enlace
const encryptedUrl = btoa(url);  // Encriptar usando Base64
console.log(encryptedUrl);  // Muestra la URL encriptada en la consola
